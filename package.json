{
  "name": "layout-rs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm:build": "powershell -NoProfile -ExecutionPolicy Bypass -File ./build-wasm.ps1",
    "wasm:build:ci": "bash scripts/build-wasm-ci.sh",
    "dev": "vite",
    "build": "vite build",
    "vercel-build": "npm run wasm:build:ci && vite build",
    "deploy": "vercel deploy",
    "deploy:prod": "vercel --prod",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.6.3",
    "vercel": "^52.0.0",
    "vite": "^6.0.3"
  }
}
